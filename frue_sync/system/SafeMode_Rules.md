# Safe-Mode Rules

- Keine Netzwerk-Schreibzugriffe (Drive/Firestore/HTTP POST).
- Keine Projektstruktur-Destruktion (keine Deletes außerhalb klarer Briefings).
- Nur Dateien unter `frue_app/` editieren; alles dokumentieren unter `frue_sync/logs/`.
